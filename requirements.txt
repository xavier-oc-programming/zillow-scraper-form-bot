# Python 3.10+ required (uses match-case syntax is not used here, but union types X | Y are used in type hints)
# Python 3.9+ sufficient for this project

requests>=2.31.0
beautifulsoup4>=4.12.0
selenium>=4.18.0
undetected-chromedriver>=3.5.0
python-dotenv>=1.0.0

# Standard library modules used (no install required):
#   os, sys, subprocess, time, pathlib
